name: Create Zip Release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}  # Use PAT for checkout
      
      - name: Configure Git
        run: |
          git config user.name "redwolfeey"  # Use YOUR username instead of bot
          git config user.email "${{ secrets.GIT_EMAIL }}"
      
      - name: Set tag for manual trigger
        if: github.event_name == 'workflow_dispatch'
        run: |
          git tag ${{ github.event.inputs.tag_name }}
          git push https://x-access-token:${{ secrets.RELEASE_TOKEN }}@github.com/redwolfeey/mythic-radio.git ${{ github.event.inputs.tag_name }}
      
      - name: Archive Release
        uses: thedoctor0/zip-release@main
        with:
          type: 'zip'
          filename: 'release.zip'
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: release.zip
          tag_name: ${{ github.event.inputs.tag_name || github.ref_name }}
          token: ${{ secrets.RELEASE_TOKEN }}
